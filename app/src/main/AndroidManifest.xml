<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reza.form">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".loginactivity.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        //LOGIN ADMIN ACTIVITY
        <activity
            android:name=".loginactivity.LoginAdminActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        //LOGIN USER ACTIVITY
        <activity
            android:name=".loginactivity.LoginUserActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        //TANGGAL SURVEY AREA
        <activity
            android:name=".TanggalSurveyArea"
            android:label="@string/Tanggal_Survey" />
        //INFO KABUPATEN DAN KOTA
        <activity
            android:name=".InfoKabKota"
            android:label="@string/Info_Kab_Kota" />
        //INFO DETAIL SERVEY AREA
        <activity
            android:name=".InformasiDetailSurveyArea"
            android:label="@string/Info_detail_survey" />
        //IDENTITAS SURVEYOR
        <activity
            android:name=".Identita_Surveyor"
            android:label="@string/Identitas_Surveyor" />
        <activity android:name=".AdminActivity"/>
    </application>

</manifest>