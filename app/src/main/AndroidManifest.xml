<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reza.form">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        //LOGIN ADMIN ACTIVITY
        <activity android:name=".LoginAdminActivity" />

        //LOGIN USER ACTIVITY
        <activity android:name=".LoginUserActivity"></activity>

        //TANGGAL SURVEY AREA
        <activity
            android:name=".TanggalSurveyArea"
            android:label="@string/Tanggal_Survey" />

        //INFO KABUPATEN / KOTA
        <activity android:name=".InfoKabKota"
            android:label="@string/Info_Kab_Kota"/>



    </application>

</manifest>